language: python
python:
  - "3.7-dev"  # 3.7 development branch

install: 
         - pip install -r requirements.txt
         - pip install coverage
         - pip install coveralls
         - pip install nose
script:  
         - python test_rmw.py
         - cd C:\Users\Andrew
         - nosetest test_rmw.py --with-coverage --cover-package=app

after_success: 
               - coveralls
       




     

